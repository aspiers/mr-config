# -*- mode: sh -*-

[.config/mr]
name = mr-config
checkout =
    git clone ssh://git.adamspiers.org/home/adam/.config/mr "$MR_REPO"
    ( cd "$MR_REPO" && git checkout master )
post_checkout = git remote add github git@github.com:aspiers/mr-config.git
chain = true
fixups =
    ln -sf "$MR_REPO/home-mrconfig" ~/.mrconfig
    ln -sf "$MR_REPO/.stow-global-ignore" ~

stow       = true
stowover   = true
restow     = true
restowover = true
unstow     = true
