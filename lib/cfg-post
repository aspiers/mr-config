# -*- mode: sh -*-

fixups =
    post_script="$MR_REPO/.cfg-post.d/$MR_NAME"
    if [ -e "$post_script" ]; then
        info "Running $post_script ..."
        "$post_script"
    else
        info "$post_script did not exist - nothing to do"
    fi
